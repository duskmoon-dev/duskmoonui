---
title: Markdown Body
description: GitHub-style typography for rendered markdown content with automatic theme support
category: components
order: 50
published: true
tags: ['markdown', 'typography', 'content', 'prose']
---

import ComponentShowcase from '../../../../components/showcase/ComponentShowcase.astro';

# Markdown Body

The `.markdown-body` class provides GitHub-style typography for rendered markdown content. It automatically adapts to DuskMoonUI themes and uses CSS custom properties for consistent styling.

## Basic Usage

Wrap any rendered markdown content with the `.markdown-body` class:

<ComponentShowcase
  title="Basic Markdown Body"
  code={`<article class="markdown-body">
  <h1>Hello World</h1>
  <p>This is <strong>bold</strong> and <em>italic</em> text.</p>
  <p>Here's a <a href="#">link</a> and some <code>inline code</code>.</p>
</article>`}
/>

## Typography

### Headings

All six heading levels are styled with appropriate sizes and weights:

<ComponentShowcase
  title="Headings"
  code={`<article class="markdown-body">
  <h1>Heading 1</h1>
  <h2>Heading 2</h2>
  <h3>Heading 3</h3>
  <h4>Heading 4</h4>
  <h5>Heading 5</h5>
  <h6>Heading 6</h6>
</article>`}
/>

### Text Formatting

<ComponentShowcase
  title="Text Formatting"
  code={`<article class="markdown-body">
  <p>Regular paragraph text.</p>
  <p><strong>Bold text</strong> and <em>italic text</em>.</p>
  <p><del>Strikethrough text</del> and <mark>highlighted text</mark>.</p>
  <p>Subscript: H<sub>2</sub>O and Superscript: E=mc<sup>2</sup></p>
  <p><small>Small text for fine print.</small></p>
</article>`}
/>

### Links

Links use the primary color and show underline on hover:

<ComponentShowcase
  title="Links"
  code={`<article class="markdown-body">
  <p>Visit <a href="#">this link</a> for more information.</p>
  <p>Multiple <a href="#">links</a> in <a href="#">one paragraph</a>.</p>
</article>`}
/>

## Lists

### Unordered Lists

<ComponentShowcase
  title="Unordered Lists"
  code={`<article class="markdown-body">
  <ul>
    <li>First item</li>
    <li>Second item
      <ul>
        <li>Nested item</li>
        <li>Another nested item
          <ul>
            <li>Deep nested item</li>
          </ul>
        </li>
      </ul>
    </li>
    <li>Third item</li>
  </ul>
</article>`}
/>

### Ordered Lists

<ComponentShowcase
  title="Ordered Lists"
  code={`<article class="markdown-body">
  <ol>
    <li>First step</li>
    <li>Second step
      <ol>
        <li>Sub-step a</li>
        <li>Sub-step b
          <ol>
            <li>Detail i</li>
            <li>Detail ii</li>
          </ol>
        </li>
      </ol>
    </li>
    <li>Third step</li>
  </ol>
</article>`}
/>

### Definition Lists

<ComponentShowcase
  title="Definition Lists"
  code={`<article class="markdown-body">
  <dl>
    <dt>Term 1</dt>
    <dd>Definition for term 1.</dd>
    <dt>Term 2</dt>
    <dd>Definition for term 2.</dd>
  </dl>
</article>`}
/>

### Task Lists

<ComponentShowcase
  title="Task Lists"
  code={`<article class="markdown-body">
  <ul>
    <li class="task-list-item">
      <input type="checkbox" checked disabled> Completed task
    </li>
    <li class="task-list-item">
      <input type="checkbox" disabled> Pending task
    </li>
    <li class="task-list-item">
      <input type="checkbox" disabled> Another pending task
    </li>
  </ul>
</article>`}
/>

## Code

### Inline Code

<ComponentShowcase
  title="Inline Code"
  code={`<article class="markdown-body">
  <p>Use the <code>npm install</code> command to install dependencies.</p>
  <p>The <code>useState</code> hook returns a <code>[state, setState]</code> pair.</p>
</article>`}
/>

### Code Blocks

<ComponentShowcase
  title="Code Blocks"
  code={`<article class="markdown-body">
  <pre><code>function greet(name) {
  console.log(\`Hello, \${name}!\`);
}

greet('World');</code></pre>
</article>`}
/>

### Keyboard Input

<ComponentShowcase
  title="Keyboard Input"
  code={`<article class="markdown-body">
  <p>Press <kbd>Ctrl</kbd> + <kbd>C</kbd> to copy.</p>
  <p>Use <kbd>Cmd</kbd> + <kbd>Shift</kbd> + <kbd>P</kbd> to open the command palette.</p>
</article>`}
/>

## Tables

Tables include header styling and zebra striping:

<ComponentShowcase
  title="Tables"
  code={`<article class="markdown-body">
  <table>
    <thead>
      <tr>
        <th>Method</th>
        <th>Description</th>
        <th>Returns</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>init()</code></td>
        <td>Initialize the library</td>
        <td>void</td>
      </tr>
      <tr>
        <td><code>configure()</code></td>
        <td>Set configuration options</td>
        <td>Config</td>
      </tr>
      <tr>
        <td><code>destroy()</code></td>
        <td>Clean up resources</td>
        <td>void</td>
      </tr>
    </tbody>
  </table>
</article>`}
/>

## Blockquotes

<ComponentShowcase
  title="Blockquotes"
  code={`<article class="markdown-body">
  <blockquote>
    <p>This is a blockquote. It can contain multiple paragraphs.</p>
    <p>Here's the second paragraph.</p>
  </blockquote>
</article>`}
/>

### Nested Blockquotes

<ComponentShowcase
  title="Nested Blockquotes"
  code={`<article class="markdown-body">
  <blockquote>
    <p>Outer blockquote.</p>
    <blockquote>
      <p>Nested blockquote.</p>
    </blockquote>
  </blockquote>
</article>`}
/>

## Images

Images are responsive and won't overflow the container:

<ComponentShowcase
  title="Images"
  code={`<article class="markdown-body">
  <figure>
    <img src="https://picsum.photos/600/300" alt="Sample image">
    <figcaption>An example image with caption</figcaption>
  </figure>
</article>`}
/>

## Horizontal Rules

<ComponentShowcase
  title="Horizontal Rules"
  code={`<article class="markdown-body">
  <p>Content above the rule.</p>
  <hr>
  <p>Content below the rule.</p>
</article>`}
/>

## Details/Summary

<ComponentShowcase
  title="Details/Summary"
  code={`<article class="markdown-body">
  <details>
    <summary>Click to expand</summary>
    <p>This is the hidden content that appears when expanded.</p>
    <p>It can contain any markdown elements.</p>
  </details>
</article>`}
/>

## Abbreviations

<ComponentShowcase
  title="Abbreviations"
  code={`<article class="markdown-body">
  <p>The <abbr title="HyperText Markup Language">HTML</abbr> specification is maintained by the <abbr title="World Wide Web Consortium">W3C</abbr>.</p>
</article>`}
/>

## Complete Example

A full document demonstrating all elements:

<ComponentShowcase
  title="Complete Document"
  code={`<article class="markdown-body">
  <h1>Project README</h1>

  <p>Welcome to the project. Here's what you need to know:</p>

  <h2>Installation</h2>

  <pre><code>npm install my-package</code></pre>

  <h2>Features</h2>

  <ul>
    <li>Feature one</li>
    <li>Feature two</li>
    <li>Feature three</li>
  </ul>

  <h2>API Reference</h2>

  <table>
    <thead>
      <tr>
        <th>Method</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>init()</code></td>
        <td>Initialize the library</td>
      </tr>
      <tr>
        <td><code>destroy()</code></td>
        <td>Clean up resources</td>
      </tr>
    </tbody>
  </table>

  <blockquote>
    <p><strong>Note:</strong> This is important information.</p>
  </blockquote>
</article>`}
/>

## Import Options

### Full Library

```css
@import "@duskmoon-dev/core";
```

### Individual Component

```css
@import "@duskmoon-dev/core/components/markdown-body";
```

## Container Sizing

For optimal readability, constrain the container width:

```css
.markdown-body {
  max-width: 980px;
  margin-inline: auto;
  padding-inline: 1rem;
}
```

## Theme Support

The component automatically adapts to DuskMoonUI themes:

```html
<!-- Light theme -->
<html data-theme="sunshine">
  <article class="markdown-body">...</article>
</html>

<!-- Dark theme -->
<html data-theme="moonlight">
  <article class="markdown-body">...</article>
</html>
```

## With Markdown Parsers

### Using marked.js

```javascript
import { marked } from 'marked';

const markdown = `
# Title
Some **bold** text and \`inline code\`.
`;

document.querySelector('.markdown-body').innerHTML = marked(markdown);
```

### Using React Markdown

```jsx
import ReactMarkdown from 'react-markdown';

function MarkdownPage({ content }) {
  return (
    <article className="markdown-body">
      <ReactMarkdown>{content}</ReactMarkdown>
    </article>
  );
}
```

### Using Astro

```astro
---
import { Content } from './my-post.md';
---

<article class="markdown-body">
  <Content />
</article>
```

## Styled Elements

| Element | CSS Selector |
|---------|-------------|
| Headings | `h1`, `h2`, `h3`, `h4`, `h5`, `h6` |
| Paragraphs | `p` |
| Links | `a` |
| Bold/Italic | `strong`, `em`, `b`, `i` |
| Strikethrough | `del`, `s` |
| Inline Code | `code` |
| Code Blocks | `pre`, `pre > code` |
| Keyboard Input | `kbd` |
| Lists | `ul`, `ol`, `li` |
| Definition Lists | `dl`, `dt`, `dd` |
| Task Lists | `.task-list-item` |
| Tables | `table`, `th`, `td` |
| Blockquotes | `blockquote` |
| Horizontal Rules | `hr` |
| Images | `img` |
| Figures | `figure`, `figcaption` |
| Abbreviations | `abbr` |
| Highlights | `mark` |
| Details/Summary | `details`, `summary` |

## Accessibility

- All colors use DuskMoonUI tokens that meet WCAG AA contrast requirements
- Focus states are clearly visible for keyboard navigation
- Reduced motion is respected via `prefers-reduced-motion` media query
- Semantic HTML elements are styled appropriately

## Related Components

- [Table](/duskmoonui/docs/en/components/table/) - Standalone table component
- [Card](/duskmoonui/docs/en/components/card/) - Content container
- [Divider](/duskmoonui/docs/en/components/divider/) - Section separator

## See Also

- [GitHub Markdown CSS](https://github.com/sindresorhus/github-markdown-css) - Inspiration for this component
- [Quick Start Guide](/duskmoonui/docs/en/getting-started/quick-start/)
